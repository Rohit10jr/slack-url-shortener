'''
nylas.com 

command = /scheduleme
request url = http:aojsouba/scheduleme
show desciption =  add event in calender
usage hint =  “title” “start date & time” “end date & time”.
example  = /scheduleme "Dinner with James" "Tomorrow at 7pm" "Tomorrow at 9pm"

raw_text = request.form.get('text') 
text_array = re.findall(r'["“”‘’'](.*?)["“”‘’']', raw_text)
text_array = re.findall(r'"(.*?)"', raw_text)


It is important to note that this text is wrapped in single quotes with each of the three components wrapped in double quotes. 
'''